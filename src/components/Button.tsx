import styled from 'styled-components';

const Button = styled.button`
    font-size: 20px;
    padding: 10px 20px;
    outline: none;
    border: none;
    margin-right: 10px;
`;

export default Button;
